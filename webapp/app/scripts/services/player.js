'use strict';

angular.module('dscover.me')
  .factory('player', function ($http, audio, fetchTracks) {

    var player,
        paused = false,
        playing = false,
        current = 0;

    // Public API here
    return {

    };
  });
